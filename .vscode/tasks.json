{
// Vea https://go.microsoft.com/fwlink/?LinkId=733558 
    // para la documentación acerca del formato tasks.json
    "version": "2.0.0",
    "tasks": [
        {
            "type":"shell",
            "label": "build.bat",
            "command":".\\shell.bat && .\\build.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}